<table class='table'>
  <tr><%= image_tag user.get_profile_image, size:'100x100' %></tr>
  <tr>
	  <th>name</th>
	  <th><%= user.name %></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction %></th>
  </tr>
  <tr id="relationship_follow">
    <%= render 'relationships/follow_counter', user: user %>
  </tr>
  <tr id="relationship_follower">
    <%= render 'relationships/follower_counter', user: user %>
  </tr>
</table>
　
<div class='row'>
  <% if current_user != user %>
    <td id="relationship-btn_info">
      <%= render 'relationships/btn', user: user, options: { class: "" } %>
    </td>
  <% else %>
    <%= link_to edit_user_path(user), class: "btn btn-outline-secondary btn-block" do %>
      <i class="fas fa-user-cog"></i>
    <% end %>
  <% end %>
</div>