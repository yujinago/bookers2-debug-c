<%= form_with model: [book, book_comment], local: false do |f| %>
    <div class="form-group">
        <%= f.text_area :comment, rows: '5', class: 'form-control book_comment', id: "comment_textarea" %>
    </div>
    <div class="form-group">
        <%= f.submit "送信" %>
    </div>
<% end %>